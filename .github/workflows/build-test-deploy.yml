name: CI/CD
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  build-test-deploy-release:
    name: CI/CD
    uses: austenstone/.github/.github/workflows/angular.yml@main
    secrets: inherit
